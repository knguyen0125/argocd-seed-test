apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "knative-operator.fullname" . }}-test-connection"
  labels:
    {{- include "knative-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "knative-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
